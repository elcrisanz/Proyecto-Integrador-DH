<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head.ejs') %>
  <title>Edición de producto</title>
</head>
<body>
<!-- Header -->
<%- include('../partials/header.ejs') %> 
<!-- Main -->
    <div class="product-form-box" >

        <!--<i class="far fa-times-circle"></i>!-->
        <h3>Editar Producto</h3>
        <form action="/products/editar/<%=productoAEditar.id%>?_method=PUT" method="POST" class="product-form" enctype="multipart/form-data">
                <div class="form-floating mb-2">
                <input type="text" name= "name" class="form-control" id="registroNombre" placeholder="Nombre" value="<%= productoAEditar.name %>"></input>
                <label for="registroNombre">Nombre:</label>
            </div>
            <div class="form-floating mb-2">
                <input type="number" name= "price" class="form-control" id="registroPrecio" placeholder="Precio" value="<%= productoAEditar.price %>"></input>
                <label for="registroPrecio">Precio:</label>
            </div>
            
            <div>
                <select class="form-select" aria-label="Tipo Producto" name="category">
                    
                    <% for (let i = 0; i<categories.length; i++) { %> 
                       <%  if (categories[i].id == productoAEditar.id_category) { %>
                        <option value = '<%= productoAEditar.id_category %> 'selected><%= productoAEditar.category.category %> </option> 
                        <% } else {%> 
                        <option value="<%= categories[i].id %> "><%= categories[i].category %></option>
                        <%}  %> 
                    <%}  %>     
                </select>
            </div>
            <br>
            <div class="archivo"action="/action_page.php">
                <label for="myfile">Cambiar foto actual: </label>
                <input type="file" id="fotoProducto" name="fotoProducto" value="<%= productoAEditar.fotoProducto %>">
                <br><br>
            </div>
            <br>
            <!-- <h6 style="font-weight: bold;">Unidades disponibles por medidas:</h6>
            <% for (let i = 0; i<measures.length; i++) { %>
                <div class="form-floating mb-2">   
                    <input type="number" class="form-control" id="registroSStock" name= "<%= measures[i].size %> Stock" placeholder="SStock" value="0"></input>
                    <label for="registroSStock"><%= measures[i].size%>  Stock:</label>
                </div>
                <% } %>  -->
        
            <div class="enviar">
                <button type="submit">Enviar</button>
            </div>
            <br>
            <div class="enviar">
                <button type="reset">Borrar Datos</button>
            </div>
        </form>
        <!-- <div>
            <p class="tenescuenta">Ya tenés cuenta?  <a href="/Login">Iniciá sesión acá</a></p>
        </div> -->
    </div>
<!-- Footer -->
<%- include('../partials/footer.ejs') %>     
</body>
</html>
