<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>
    <link href="css/estilos.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Registro</title>
</head>
<body>
    <div class="contenedor">
        <!--<i class="far fa-times-circle"></i>!-->
        <h3>Crea tu cuenta</h3>
        <form action="/user/registro" method="POST" enctype="multipart/form-data">
        
            <div class="form-floating mb-2">
                <input type="text" class="form-control" id="registroNombre" placeholder="Nombre" name="name" value="<%= locals.oldData ? oldData.name : null %>"></input>
                <label for="registroNombre">Nombre</label>
                <% if (locals.errors && errors.name){ %>
                <div class="text-danger">
                 <%= errors.name.msg %>
                </div>
                <% } %>
            </div>
            <div class="form-floating mb-2">
                <input type="text" class="form-control" id="registroApellido" placeholder="Apellido" name="lastName" value="<%= locals.oldData ? oldData.lastName : null %>"></input>
                <label for="registroApellido">Apellido</label>
                <% if (locals.errors && errors.lastName){ %>
                    <div class="text-danger">
                     <%= errors.lastName.msg %>
                    </div>
                    <% } %>
            </div> 
            <div class="form-floating mb-2">
                <input type="password" class="form-control" id="registroContraseña" placeholder="Contraseña" name="password" value="<%= locals.oldData ? oldData.password : null %>"></input>
                <label for="registroContraseña">Contraseña</label>
                <% if (locals.errors && errors.password){ %>
                    <div class="text-danger">
                     <%= errors.password.msg %>
                    </div>
                    <% } %>
            </div>
            <div class="form-floating mb-2">
                <input type="password" class="form-control" id="registroConfirmaContraseña" placeholder="Confirmar contraseña" name="passwordConfirm" value="<%= locals.oldData ? oldData.passwordConfirm : null %>" ></input>
                <label for="registroConfirmaContraseña">Confirmar contraseña</label>
                <% if (locals.errors && errors.passwordConfirm){ %>
                    <div class="text-danger">
                     <%= errors.passwordConfirm.msg %>
                    </div>
                    <% } %>
            </div>
            <div class="form-floating mb-2">
                <input type="email" class="form-control" id="registroEmail" placeholder="Correo electrónico" name="mailRegistro" value="<%= locals.oldData ? oldData.mail : null %>"></input>
                <label for="registroEmail">Email</label>
                <% if (locals.errors && errors.mailRegistro){ %>
                    <div class="text-danger">
                     <%= errors.mailRegistro.msg %>
                    </div>
                    <% } %>
            </div>
            <div class="form-floating">
                <input type="email" class="form-control" id="registroConfirmEmail" placeholder="Confirma correo electrónico:" name="emailConfirm" value="<%= locals.oldData ? oldData.emailConfirm : null %>"></input>
                <label for="registroConfirmEmail">Confirmar Email</label>
                <% if (locals.errors && errors.emailConfirm){ %>
                    <div class="text-danger">
                     <%= errors.emailConfirm.msg %>
                    </div>
                    <% } %>
            </div>
            <br>
            <div class="form-float">
                <label for="avatar">Carga tu foto de perfil: </label>
                <br>               
                <input type="file" id="avatar" name= "avatar">
                <% if (locals.errors && errors.avatar){ %>
                    <div class="text-danger">
                     <%= errors.avatar.msg %>
                    </div>
                    <% } %>
            </div> 
            <br>
            <div class="crear">
                <button type="submit" id="registro">Crear Cuenta</button>   
            </div>
        </form>
        <div>
            <p class="tenescuenta">Ya tenés cuenta?  <a href="/Login">Iniciá sesión acá</a></p>
        </div>
    </div>
    
</body>
</html>